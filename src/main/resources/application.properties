# Camunda 8 connection (Zeebe / Operate / Tasklist)
camunda.client.grpc-address=grpcs://<cluster-id>.<region>.zeebe.camunda.io:443
camunda.client.rest-address=https://<region>.zeebe.camunda.io/<cluster-id>
camunda.client.auth.client-id=<client-id>
camunda.client.auth.client-secret=<client-secret>

# Spring application name
spring.application.name=braket-connector

# Logging
logging.level.de.envite.connector.braket=DEBUG

# gRPC keepalive – sends a ping every 30 s so dead connections are detected
# quickly and the Zeebe client reconnects before job workers stall.
camunda.client.keep-alive=PT30S

# Actuator – expose the health endpoint for Docker liveness probes.
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
